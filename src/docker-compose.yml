version: '3.4'

services:
  eshopdb:
    image: postgres

  distributedcache:
    image: redis
    

  seq:
    image: datalust/seq:latest
    environment:
      - ACCEPT_EULA=Y
      - SEQ_FIRSTRUN_ADMINPASSWORD=12345


volumes:
  postgres_eshopdb: